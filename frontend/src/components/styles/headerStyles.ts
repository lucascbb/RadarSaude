const headerStyle: React.CSSProperties = {
    display: "flex",
    backgroundColor: '#fffaff',
    borderBottom: '1px solid #c0c0c0',
    justifyContent: "space-between",
    alignItems: "center",
    padding: "0 30px"
};

const mainPageStyle: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: "5px",
}

const iconHomeStyle: React.CSSProperties = {
    maxHeight: "32px"
}

const pageTextStyle: React.CSSProperties = {
    fontSize: "15px",
    lineHeight: "18px"
}

const mainDataUserStyle: React.CSSProperties = {
    display: "flex",
    gap: "15px",
    alignItems: "center",
}

const pictureProfileStyle: React.CSSProperties = {
    width: "30px",
    height: "30px",
    borderRadius: "50%",
    objectFit: "cover"
}

const nameTextStyle: React.CSSProperties = {
    fontSize: "15px",
    maxWidth: "170px",
    lineHeight: "18px"
}

export { headerStyle, mainPageStyle, iconHomeStyle, pageTextStyle, mainDataUserStyle, pictureProfileStyle, nameTextStyle }

